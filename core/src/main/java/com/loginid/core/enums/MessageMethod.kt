package com.loginid.core.enums

/**
 * Represents the delivery method for a message, such as a one-time password (OTP).
 */
enum class MessageMethod {
    /**
     * Delivery via email.
     */
    EMAIL,

    /**
     * Delivery via SMS (Short Message Service).
     */
    SMS
}
